apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: memory-recall-embedding-cache
  namespace: memory-recall-coin
  labels:
    app.kubernetes.io/name: memory-recall-embedding
    app.kubernetes.io/component: embedding
    app.kubernetes.io/part-of: memory-recall-coin
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: memory-recall-embedding
  namespace: memory-recall-coin
  labels:
    app.kubernetes.io/name: memory-recall-embedding
    app.kubernetes.io/component: embedding
    app.kubernetes.io/part-of: memory-recall-coin
spec:
  replicas: 1
  revisionHistoryLimit: 2
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: memory-recall-embedding
      app.kubernetes.io/component: embedding
  template:
    metadata:
      labels:
        app.kubernetes.io/name: memory-recall-embedding
        app.kubernetes.io/component: embedding
        app.kubernetes.io/part-of: memory-recall-coin
    spec:
      automountServiceAccountToken: false
      enableServiceLinks: false
      nodeSelector:
        kubernetes.io/hostname: k3s-master
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: text-embeddings-inference
          image: ghcr.io/huggingface/text-embeddings-inference@sha256:ad950d30878eceb72aaf32024d26fa2b1d04a75304fa0b4776b49aa1941fea07
          imagePullPolicy: IfNotPresent
          args:
            - --model-id
            - BAAI/bge-small-zh-v1.5
            - --revision
            - 7999e1d3359715c523056ef9478215996d62a620
            - --served-model-name
            - bge-small-zh-v1.5@7999e1d
            - --dtype
            - float32
            - --hostname
            - 0.0.0.0
            - --port
            - "8080"
            - --huggingface-hub-cache
            - /data
            - --max-concurrent-requests
            - "16"
            - --max-batch-tokens
            - "512"
            - --max-batch-requests
            - "2"
            - --max-client-batch-size
            - "8"
            - --json-output
          env:
            - name: HF_HUB_DISABLE_TELEMETRY
              value: "1"
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          resources:
            requests:
              cpu: 500m
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 1Gi
          startupProbe:
            httpGet:
              path: /health
              port: http
            failureThreshold: 120
            periodSeconds: 5
            timeoutSeconds: 2
          readinessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 30
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
          volumeMounts:
            - name: model-cache
              mountPath: /data
            - name: runtime
              mountPath: /tmp
      volumes:
        - name: model-cache
          persistentVolumeClaim:
            claimName: memory-recall-embedding-cache
        - name: runtime
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: memory-recall-embedding
  namespace: memory-recall-coin
  labels:
    app.kubernetes.io/name: memory-recall-embedding
    app.kubernetes.io/component: embedding
    app.kubernetes.io/part-of: memory-recall-coin
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: memory-recall-embedding
    app.kubernetes.io/component: embedding
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: memory-recall-embedding
  namespace: memory-recall-coin
  labels:
    app.kubernetes.io/name: memory-recall-embedding
    app.kubernetes.io/component: embedding
    app.kubernetes.io/part-of: memory-recall-coin
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: memory-recall-embedding
      app.kubernetes.io/component: embedding
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: memory-recall-coin
              app.kubernetes.io/component: server
      ports:
        - port: 8080
          protocol: TCP
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: monitoring
      ports:
        - port: 8080
          protocol: TCP
